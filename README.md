# setup-qwen-with-claude-ccr
This repository provides a step-by-step guide to configure Qwen (via Qwen Code CLI) with Claude Code Router (CCR) so you can use Qwen models inside Claude Code workflows.  The guide is written for Windows (PowerShell) users and assumes basic familiarity with Node.js and CLI tools.
